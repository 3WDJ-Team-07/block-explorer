import React from 'react'

const Transactions = ({ match, history }) => {
    console.log(match);
    console.log(history);
    return (
        <div>
            Transactions page
        </div>
    );
}

export default Transactions
